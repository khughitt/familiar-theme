# familiar-theme

The theme contract package: the shared protocol that `familiar` (engine) and
`familiar-forge` both depend on. Ships no art of its own.

- Run tests: `npm test`
- Pre-split history: https://github.com/khughitt/familiar-archive
